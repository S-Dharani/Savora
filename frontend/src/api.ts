import axios from 'axios'; export const api=axios.create({baseURL:import.meta.env.VITE_API_URL||'http://spring-service:8084/api'}); api.interceptors.request.use(c=>{const t=localStorage.getItem('access');if(t)c.headers.Authorization=`Bearer ${t}`;return c}); export const get=<T,>(url:string)=>api.get<{data:T}>(url).then(r=>r.data.data);
